package forcomp

object Anagrams {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(59); val res$0 = 
  forcomp.loadDictionary;System.out.println("""res0: List[String] = """ + $show(res$0))}
  
}